import React from 'react';

import Layout from '../components/layout/Layout';

const Countdown = () => {
    return (
        <Layout>
            Countdown
        </Layout>
    );
};

export default Countdown;