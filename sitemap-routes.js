import React from 'react';
import { Routes, Route } from 'react-router';

export default (
    <Routes>
        <Route path='/' />
        <Route path='/notepad' />
        <Route path='/countdown' />
    </Routes>
);
